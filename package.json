{
  "name": "ashley-calendar-ai",
  "version": "1.0.0",
  "description": "An intelligent calendar assistant",
  "main": "index.js",
  "scripts": {
    "test": "node --import tsx --test test-*.ts",
    "test:smoke": "grep -l '@smoke' test-*.ts | xargs node --import tsx --test",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@boundaryml/baml": "^0.204.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "eslint": "^8.0.0"
  },
  "keywords": ["calendar", "ai", "assistant"],
  "author": "Sid Mathur",
  "license": "MIT"
}